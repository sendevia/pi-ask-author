# Pi 插件开发与编码规范 (Extensions Development Guidelines)

> 本文档适用于 `.pi/extensions/` 目录下所有 Pi 扩展插件的设计、重构与开发。
> 所有代码提交与修改均须严格遵循本规范，确保交付高质量、高可读、高性能的生产级代码。

---

## 一、 核心设计哲学与架构规范

### 1.1 最优实现与优雅代码 (Optimal & Elegant)

- **极简且健壮**：用最少、最清晰的代码表达最强壮的业务逻辑。杜绝无意义的过度抽象（Over-engineering）与意大利面条式代码。
- **高内聚分层组织**：小型插件以独立单文件形式组织，代码内部按逻辑严格自顶向下分层；超过约 1500 行或功能模块边界清晰的中大型插件，应拆分为目录式扩展（`.pi/extensions/<name>/index.ts` 入口 + 单向依赖的模块文件），依赖关系必须无环。
  1. **文件级 TSDoc 与模块导入**：清晰标注插件用途、特性与依赖导入（Pi 核心库、TypeBox、TUI 组件）。
  2. **常量配置、终端图标与文案字典**：集中管理布局常量（`LAYOUT_CONFIG`）、Nerd Font 图标（`ICONS`）与多语言/双向文案（`TEXTS`）。
  3. **TypeBox 参数 Schema 与 TypeScript 类型推导**：通过 `Type.Object` 定义工具输入，利用 `Static<typeof Schema>` 单一源头派生类型。
  4. **纯函数算法与排版工具层**：视口计算、文本行折行、ANSI 宽度测量、小说台词/Markdown 高亮解析。
  5. **TUI 状态机与交互组件层**：实现 `Component` 与 `Focusable` 接口，包含按键响应、光标记忆、IME 硬件光标定位与双栏渲染。
  6. **插件入口与生命周期集成**：`export default function(api: ExtensionAPI): void`，注册 Tool、Command 与 Renderer。
- **卫语句与扁平控制流**：优先使用早期返回（Early Return / Guard Clauses），消除多层深嵌套 `if-else`。

### 1.2 强类型与静态安全 (Strict Typing)

- **零 `any` 容忍**：严格开启 TypeScript Strict 模式，杜绝隐式或显式 `any`；必要时使用 `unknown` 并配合类型守卫（Type Narrowing）。
- **单一类型源（Single Source of Truth）**：Schema 与类型定义严格绑定，字段名称、可选性与默认值必须在 TypeBox Schema 中一次性定义，避免手动重复维护冗余 interface。
- **不可变性优先**：配置对象与状态常量使用 `as const` 修饰，避免运行时意外修改。

---

## 二、 运行效率与零冗余准则 (Performance & Zero Redundancy)

### 2.1 渲染与计算高效化

- **轻量纯粹的 `render()`**：
  - TUI 组件的 `render(width)` 方法每帧均可能被调用，**严禁**在 `render()` 内执行重计算、深拷贝、正则解析或 I/O 操作。
  - 所有需要折行计算的文本、富文本解析结果，必须在数据变更（State Mutation / Cursor Change）时预先计算并缓存。
- **视口滑动窗口算法 ($O(1)$)**：
  - 对长列表或草稿预览，采用紧凑的双向边界滑动窗口算法（`scrollOffset` 计算），仅切片当前视口行渲染，不遍历非可视区数据。
- **ANSI 宽度与字符保护**：
  - 终端字符串截断与对齐必须使用 `visibleWidth`、`truncateToWidth` 与 `wrapTextWithAnsi`，杜绝直接通过 `string.length` 处理中文字符与 ANSI 转义序列导致排版错乱。

### 2.2 内存与生命周期管理

- **定时器与监听器 100% 清理**：
  - 所有 `setTimeout`、`setInterval` 状态提示以及 `process.stdout.on('resize')` 监听，必须在组件卸载或 `resolve()` 时安全回收，防止内存泄漏或幽灵事件。
- **AbortSignal 级联响应**：
  - 异步工具执行必须监听 `ExtensionContext.signal`，当任务被用户取消（Esc / Ctrl+C）时立即中断并清理资源。

### 2.3 零代码冗余 (Strict DRY)

- **消除死代码与重复逻辑**：严禁残留调试打印（`console.log`）、注释掉的历史代码或未引用的工具函数。
- **公共逻辑抽离**：同类文本处理、格式化、状态校验统一收敛至纯函数工具集中，禁止在各事件分支复制代码片段。

---

## 三、 TSDoc 编写标准 (Complete, Intuitive & Uniform TSDoc)

所有导出的类型、常量、工具函数及组件类必须具备完整、直观、格式规范的 TSDoc 注释，使代码自解释。同一插件目录内的注释风格必须**逐项统一**，禁止逐文件自行其是。

### 3.1 铁律：只描述当前事实 (Present-Tense Only)

- **禁止更新痕迹**：注释只写「现在是什么、为何如此设计」，严禁任何描述变更过程的叙述。
  - ✗ 违例：`切断历史循环依赖`、`与原内联实现逐字节一致`、`刻意保留的防御性冗余`、`兼容历史会话写法`、`本次新增`、`（原为…）`、`此前`、`已修复`、`待补`。
  - ✓ 合规：直接陈述当前约束与契约——`不得导入插件内其他模块`、`文案与几何一律取自 texts.ts`、`本层再幂等兜底一次`、`兼容未按当前 Schema 书写的入参`。
- **禁止复述代码**：内联注释只解释**为什么**（非显然的约束、宿主契约、性能取舍），删除解释「是什么」的旁白。
- **禁止死注释**：不留 `TODO` / `FIXME` / 注释掉的历史代码——要么当轮实现，要么删除（§2.3）。
- **术语特例**：`原文`（raw text，与处理后文本相对）等描述**数据形态**的词不算更新痕迹，可正常使用。

### 3.2 结构与覆盖范围

1. **文件头部注释**（模块定位的唯一入口说明）：

   ```ts
   /**
    * @file <文件名>
    * @description <一句话定位：本模块负责什么>
    *
    * 核心架构特性：
    * - <2~5 条：架构模式、算法与性能契约、关键不变量>
    *
    * 依赖方向：<本模块 → 下游模块；外部依赖；禁止的反向导入>
    */
   ```

   - 入口文件另须在 `依赖方向` 一行给出完整模块依赖图（自底向上 + 旁支模块）。
   - 模块特有的全局约定（如「索引一律为 UTF-16 码元下标」、「宿主主题耦合」）单列一行，不塞进 `核心架构特性` 列表。

2. **常量与文案字典**：单行标注含义、度量单位（`ms` / `cols` / `lines` / `项` / `题`）、数量约束及其约束对象（如 `同时约束 schema.ts 的 maxItems`）。
3. **TypeBox Schema 与类型字段**：单行说明业务含义与必填性、默认值、渲染参与范围；统一写作 `（必填；…）` / `（可选；…）` / `（默认 false）`，细节以 `；` 分隔。
4. **函数与类方法**：
   - **摘要行**：单句、动宾结构、**句末不加句号**（如 `将数值收敛至闭区间 [min, max]`）。
   - **补充说明**：紧随摘要行另起一行（**不空行**），统一使用标签前缀 `不变量：` / `复杂度：` / `边界：` / `契约：` / `语言归属：` / `宿主主题耦合：`；整块不超过 1~2 句，禁止多段长文与编号清单堆砌。
   - **`@param` 全覆盖**：每个参数一行，格式 `@param name - 含义（约束条件 / 单位 / 缺省行为）`。
   - **`@returns` 全覆盖**：返回语义不自明的函数、方法与 getter 必写，**单行不折行**，必须含边界情形（越界 / 空输入 / 未命中时返回什么）；纯派生 getter（如 `get questionCount()`）可由摘要行直接承载返回语义。
   - 核心算法须写明不变量、复杂度与放弃条件（如最坏情形退化、超限即放弃高亮）。
5. **组件类**：类注释给出状态机职责、状态突变入口与生命周期不变量（`dispose()` 幂等、`done` 至多投递一次、渲染纯度边界）。
6. **引用与排版**：
   - 标识符、字面量、按键名、API 名一律以反引号包裹；跨模块或同文件符号引用使用 `{@link}`。
   - 分节统一为 `// ===== 标题 =====`，同一文件内保持一致。
   - 中文标点统一：句中用 `，` / `、` / `；`，与英文术语混排不加空格，**全块句末均不加句号**。

---

## 四、 双向字典文案设计规范 (Dual-Audience Copywriting)

插件系统承载着 **用户交互 (TUI/CLI/Notifications)** 与 **大语言模型理解 (LLM Schemas/Prompts/Envelopes)** 两组目标受众。所有文案必须在 `TEXTS` 字典中集中维护，并遵循各自的受众准则：

### 4.1 用户端文案 (User-Facing Copy) —— 直观、短促、行动导向

- **去繁就简**：消除冗余客套用语与助词，提炼为动词引导的高浓度操作提示。
- **图标与按键规范**：
  - 统一结合语义化终端图标增强状态识别。
  - 按键与操作引导统一采用标准按键名称与简明动宾结构。
- **明确的阻断提示（Actionable Errors）**：
  - 错误与阻断通知必须明确指出未满足的约束条件与具体的纠偏操作路径。

### 4.2 LLM 端文案 (LLM-Facing Copy) —— 全英文、高信噪比与 Token 经济

所有面向大语言模型（LLM）的提示词文案——包括 `TEXTS.tool.description`、`TEXTS.tool.promptSnippet`、`TEXTS.tool.promptGuidelines`、`TEXTS.schema` 字段描述、`TEXTS.markdown` 返回封套与纠偏指令——**必须 100% 统一采用地道、精确、高密度的英文**。

#### 核心设计考量与收益

1. **Token 经济与缓存极致化 (Token Economy & Cache Optimization)**：
   - 现代主流分词器（BPE / Tiktoken）对英文单词的编码效率显著高于中文字符（平均节省 30%~50% Token 开销）。
   - 纯英文 System Prompt 与 Schema 定义能大幅减小静态前缀体积，最大化 Prefix Cache / Prompt Cache 的命中率与复用寿命。
2. **指令遵循与防幻觉能力 (Strict Instruction Following & Zero Hallucination)**：
   - 主流基础大模型（Claude、GPT 系列等）在英文系统级约束与 Guidelines 下表现出更强的防御性、边界意识与严谨度，能更稳定地抑制聊天框前置寒暄与伪字段自作主张输出等坏味道行为。
3. **工具协议一致性 (Protocol Consistency)**：
   - 保持与核心内置工具体系的全局提示词风格与协议标准高度统一。

#### 编写与设计黄金准则

- **单句化与精炼精准的 Schema 描述 (`TEXTS.schema`)**：
  - 每一个字段的描述控制在一句浓缩英文内，清晰声明字段业务语义、必要性（Required/Optional）与推荐省略规则，严禁冗余叙述。
- **结构化且高密度的 Prompt Guidelines (`TEXTS.tool.promptGuidelines`)**：
  - 采用简明的分类标签前缀，直击核心行为契约；禁止冗长废话或重复描述。
  - **直接调用契约 (DIRECT Invocation)**：明确禁止在聊天框输出前置寒暄或解释，必须直接调用工具。
  - **模式与批处理 (Modes & Batching)**：明确单题平铺与多题批量问卷的参数结构，强约束禁止拆分为连续单题调用。
  - **严格省略默认值 (Omit Defaults / Payload Minimization)**：严格约束模型省略所有具有默认行为的可选字段，严禁输出无意义伪 ID 或冗余默认布尔值。
  - **选项设计与上下文 (Option Quality)**：要求提供具象且具备实质行为描述的选项标签，杜绝无意义通用占位符，并在对应字段提供因果推演与 Markdown 正文预览。
  - **多选与补充说明 (Multi-select & Notes)**：明确多选适用的语义边界，并声明自由补充说明支持机制。
  - **分镜草稿指令 (Draft Directives)**：说明草稿预览将被压缩为稠密单行指令注入结果，指导后续创作。
- **确定性返回封套与紧凑指令 (Deterministic Envelopes & Directives)**：
  - 工具返回给 LLM 的文本采用固定的结构化 Markdown 封套标识。
  - 选项结果、剧情推演与分镜指令压缩为高密度单行，统一搭配英文语义前缀与标签。
  - 尾部附带明确的后续工作流执行指令，强力杜绝模型的闲聊确认，直接驱动下一步正文写作与大纲更新。

---

## 五、 开发与交付自检清单 (Verification Checklist)

在修改或提交插件代码前，必须逐项执行以下验证：

1. **类型检查**：

   ```bash
   pnpm --dir .pi/extensions check
   ```

   确保 `tsc -p tsconfig.json` 零警告、零错误。目录式扩展必须把子目录纳入 `include`（如 `"include": ["ask-author"]`），确保拆分模块同样接受完整严格检查。

2. **注释规范审查（§3）**：
   - [ ] 注释是否只描述当前事实，无任何更新痕迹（`历史` / `此前` / `原为` / `刻意` / `逐字节` / `本次新增` 等零命中）？
   - [ ] 是否存在复述代码的旁白式内联注释或死注释（`TODO` / `FIXME` / 注释掉的代码）？
   - [ ] 每个参数是否有 `@param`、每个非平凡返回值是否有 `@returns`（含越界 / 空输入等边界情形）？
   - [ ] 文件头是否具备 `@file` / `@description` / `核心架构特性` / `依赖方向` 四段结构？
   - [ ] 摘要行是否单句且句末无句号；补充说明是否使用统一标签前缀且未超出 1~2 句？
   - [ ] 常量与字段是否标注单位、默认值与数量约束？

   机械化自检（在插件目录执行，两条均应**零输出**）：

   ```bash
   # 注释中的更新痕迹
   grep -nE '(历史|此前|原为|原先|原来|刻意|逐字节|内联实现|本次新增|已修|待补|TODO|FIXME)' *.ts | grep -E ':\s*(\*|/\*\*|//)'
   # 注释块内的句末句号
   grep -nE '。\s*$' *.ts | grep -E ':\s*(\*|/\*\*|//)'
   ```

   `@param` / `@returns` 覆盖率与文件头完整性可用一次性脚本扫描（提取每个函数签名上方的注释块，校验参数名与返回语义是否齐备），无需人工逐函数目视。

3. **其余规范审查**：
   - [ ] 是否所有文案均收敛于 `TEXTS` 字典，无硬编码中文/提示词碎片？
   - [ ] 面向智能体的提示词（Tool Description、Schema、Guidelines、Result Envelopes）是否已 100% 采用地道、精炼、浓缩的英文？
   - [ ] 提示词是否清晰规定了默认字段省略契约，以最大化 Prompt Cache 命中率并降低 Token 消耗？
   - [ ] 新增或修改的函数、类型、组件是否补充了直观完整的 TSDoc？
   - [ ] TUI 组件在极窄终端（如宽度 < 72 列）下是否有优雅的单栏降级方案？
   - [ ] 所有定时器、事件监听和 Abort 信号是否均已妥善释放？
