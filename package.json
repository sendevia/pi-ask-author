{
  "name": "@sendevia/pi-ask-author",
  "version": "1.0.3",
  "description": "面向文字创作者的决策询问 Pi 扩展",
  "keywords": [
    "pi-package",
    "pi-extension",
    "tui",
    "creative-writing",
    "novel",
    "author-consultation"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/sendevia/pi-ask-author"
  },
  "license": "MIT",
  "author": "sendevia",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && esbuild src/index.ts --bundle --format=esm --platform=node --target=ESNext --external:@earendil-works/* --external:typebox --minify --tree-shaking=true --legal-comments=none --drop:debugger --drop:console --outfile=dist/index.js && terser dist/index.js --compress passes=3,pure_getters=true --mangle --output dist/index.js",
    "prepack": "pnpm build",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.85.1",
    "@earendil-works/pi-tui": "^0.85.1",
    "esbuild": "^0.28.2",
    "terser": "^5.51.2",
    "typebox": "^1.3.31",
    "typescript": "^7.0.2"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "pi": {
    "extensions": [
      "./dist/index.js"
    ]
  }
}
